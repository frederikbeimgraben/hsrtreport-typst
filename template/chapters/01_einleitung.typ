#import "@local/hsrtreport:1.0.0": *

= Einleitung <chap:einleitung>

Die Einleitung nennt das Thema, die Fragestellung und den Aufbau der Arbeit.

== Motivation <sec:motivation>

Ein Verweis auf ein Kapitel sieht so aus: @chap:einleitung. Eine Quelle zitiert
man so: @ahrens2014.

== Aufbau der Arbeit <sec:aufbau>

Der #gls("Textkörper") enthält alle Kapitel zwischen Einleitung und Fazit.
